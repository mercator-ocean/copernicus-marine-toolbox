name: Create Binaries

on:
  push:
    branches:
       - main
       - 'pre-releases/**'
  pull_request:
    branches:
       - copernicusmarine-toolbox-v2

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        os: [windows-latest, macos-latest, ubuntu-latest]
    runs-on: ${{ matrix.os }}
    timeout-minutes: 20

    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        fetch-depth: 0

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'

    - name: Install dependencies
      id: set-version
      run: |
        make build-and-prepare-for-binary-${{ matrix.os }}


    - name: Build with PyInstaller
      run: |
        make run-using-pyinstaller-${{ matrix.os }}

    - name: Upload binaries to release
      env:
        GH_TOKEN: ${{ github.token }}
      if: matrix.os == 'windows-latest'
      run: |
          $MY_VARIABLE = poetry version --short
          gh release upload v$MY_VARIABLE dist/cmt.exe#${{matrix.os}}-copernicus.exe
    - name: Upload binaries to macos
      env:
        GH_TOKEN: ${{ github.token }}
      if: matrix.os == 'macos-latest'
      run: |
          gh release upload v${{steps.set-version.outputs.VERSION}} dist/cmt.cli#${{matrix.os}}-copernicus --clobber
    - name: Upload binaries to ubuntu
      env:
        GH_TOKEN: ${{ github.token }}
      if: matrix.os== 'ubuntu-latest'
      run: |
          gh release upload v${{steps.set-version.outputs.VERSION}} dist/cmt-ubuntu.cli#${{matrix.os}}-copernicus


      #- name: Upload binaries to release
      # uses: svenstaro/upload-release-action@v2
      # with:
      #   repo_token: ${{ secrets.GITHUB_TOKEN }}
      #   file: dist/copernicus_marine.exe
      #   asset_name: windows-binary
      #   tag: v2.0.0a0
      #   overwrite: true
